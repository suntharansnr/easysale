import styled from 'styled-components';



function SearchList({className=""}) {
  return (
    <DIV className={`${className}`}>
       
    </DIV>
  );
}


const DIV=styled.div`
    width: 100%;
  
`;

export default SearchList;
